{
  "name": "cerbos",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "copy:policies": "cp -r policies dist",
    "build": "tsc --outDir dist && npm run copy:policies",
    "start": "npm run build && node dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@cerbos/core": "^0.19.0",
    "@cerbos/files": "^0.2.11",
    "@cerbos/grpc": "^0.19.0",
    "@cerbos/http": "^0.20.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
